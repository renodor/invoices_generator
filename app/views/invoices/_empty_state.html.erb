<div class="empty-state empty-state--only-child">
  <p class="empty-state__text">
    You don't have any invoices yet!
  </p>

  <%= link_to 'Add invoice',
              new_invoice_path,
              class: 'btn btn--primary',
              data: { turbo_frame: dom_id(Invoice.new) } %>
</div>