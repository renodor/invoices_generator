<%= turbo_stream.prepend 'invoices' do %>
  <%= render partial: 'invoices/invoice', locals: { invoice: @invoice } %>
<% end %>
<%#= could be written shorten like that: "turbo_stream.prepend "invoices", @invoice" %>


<%= turbo_stream.update Invoice.new, '' %>
<%= render_turbo_stream_flash_messages %>