<main class="container">
  <%= link_to sanitize("&larr; Back to invoice"), invoice_path(@invoice) %>

  <div class="header">
    <h1>Edit item</h1>
  </div>

  <%= turbo_frame_tag @line_item do %>
    <%= render "form",
              invoice: @invoice,
              line_item_date: @line_item_date,
              line_item: @line_item %>
  <% end %>
</main>